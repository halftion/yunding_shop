<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd">
    <context:property-placeholder location="classpath:jdbc.properties , classpath:Alipay.properties ,classpath:SMS.properties"/>

    <bean id="dataSource" class="com.alibaba.druid.pool.DruidDataSource">
        <property name="driverClassName" value="com.mysql.jdbc.Driver"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
        <property name="minIdle" value="10" />
        <property name="initialSize" value="10" />
        <property name="maxActive" value="20" />
    </bean>
    
    <bean id="alipay" class="yunding.shop.util.AlipayUtil">
        <property name="appId" value="${appId}"/>
        <property name="privateKey" value="${privateKey}"/>
        <property name="format" value="${format}"/>
        <property name="publicKey" value="${publicKey}"/>
        <property name="signType" value="${signType}"/>
        <property name="charset" value="${charset}"/>
        <property name="gatewayUrl"  value="${gatewayUrl}"/>
        <property name="returnUrl" value="${returnUrl}"/>
        <property name="notifyUrl" value="${notifyUrl}"/>
        <property name="logPath" value="${logPath}"/>
    </bean>

    <bean id="sMSUtil" class="yunding.shop.util.SmsUtil">
        <property name="product" value="${SMS.product}"/>
        <property name="domain" value="${SMS.domain}"/>
        <property name="accessKeyId" value="${SMS.accessKeyId}"/>
        <property name="accessKeySecret" value="${SMS.accessKeySecret}"/>
        <property name="templateCode" value="${SMS.templateCode}"/>
        <property name="outTime" value="${SMS.outTime}"/>
    </bean>


   
    <bean id="sessionFactoryBean" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="configLocation" value="classpath:mybatis/configuration.xml"/>
        <property name="mapperLocations" value="classpath:mapper/*Mapper.xml"/>
    </bean>

    <bean id="mapperScannerConfigurer" class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="sqlSessionFactoryBeanName" value="sessionFactoryBean" />
        <property name="basePackage" value="yunding.shop.mapper"/>
    </bean>
</beans>